<?xml version="1.0" encoding="utf-8"?>
<!--
    Input-method metadata.

    One subtype per layout, mirroring KeyboardLanguage on iOS. Declaring them
    lets the system's own language switcher and the globe key's long-press list
    show AI Reply's three layouts, instead of the keyboard looking like a single
    English input method that happens to type Cyrillic.

    isDefault is deliberately absent: this keyboard does not ask to be the
    system default.
-->
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="kz.yerek.aireply.MainActivity"
    android:supportsSwitchingToNextInputMethod="true">

    <subtype
        android:icon="@drawable/ic_keyboard"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="true"
        android:label="@string/subtype_en" />

    <subtype
        android:icon="@drawable/ic_keyboard"
        android:imeSubtypeLocale="ru_RU"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="false"
        android:label="@string/subtype_ru" />

    <subtype
        android:icon="@drawable/ic_keyboard"
        android:imeSubtypeLocale="kk_KZ"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="false"
        android:label="@string/subtype_kk" />

</input-method>
